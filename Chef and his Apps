#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	    int a,b,c,d;
	    cin>>a>>b>>c>>d;
	    if(a >= b+c+d)
	    cout<<0<<endl;
	    else if(a-b-c-d < 0)
	    if(a-b>=d || a-c>=d)
	    cout<<1<<endl;
	    else if(a+b+c>d)
	    cout<<2<<endl;
	}
	return 0;
}
